#ifndef LEXICAL_HPP
#define LEXICAL_HPP

enum class lexer {
    STRING = -1,
    INTEGER = -2,
    OPERATOR = -3,
    PRINT = -4,
    VARIABLE = -5,
    LET = -6,
    STATIC = -7,
    ENUM = -8
};

#endif
